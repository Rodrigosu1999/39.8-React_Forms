//Default boxes to test React App
//As this is only an exercise and we don't have real id's from a database will work with a hardcoded number

const defaultBoxes = [
    {id: 1, color: "#0000FF", height: 100, width: 100}
];

export default defaultBoxes;